// 6. Escreva um programa em C para ler um valor X e um valor Z (se Z for menor que X deve ser lido um novo valor para Z). Imprimir o número de vezes que x é menor que Z.


#include<stdio.h>
#include<stdlib.h>

int main() {
    
    float x, z;
    float result;
    printf("Digite dois numeros:\n");
    scanf("%f\n%f", &x, &z);
    
    while (z < x) {
        printf("Z deve ser maior que X, digite outro valor para Z");
        scanf("%f", &z);
    }
    
    result = z/x;
    
    printf("%.1f é %.1f vezes menor do que %.1f\n\n", x, result, z );
    
    
    
    return 0;
    
}
